class Switcher {

	public static void main(String[] args) {
		
	}

	public static String switcher(String intial, String swapA, String swapB) {
		for (int i=0; i.swapA.length() >= intial.length; i++) {
			seg = intial.substring(i, i+swapA.length());
			if (seg.eql(swapA)) {
				// save the place for swap later
			}
		}
		for (int i=0; i.swapB.length() >= intial.length; i++) {
			seg = intial.substring(i, i+swapA.length());
			if (seg.eql(swapB))  {
				// save the place for swap later
			}
		}
		for () {


		}

	}

}